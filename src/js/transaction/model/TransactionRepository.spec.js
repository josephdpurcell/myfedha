//var TransactionRepository = require('/var/www/myfedha.com/src/js/transaction/model/TransactionRepository.js');

describe('A TransactionRepository', function() {
  it('contains a TransactionRepository', function() {
    //expect(TransactionRepository).toBeDefined();
  });

  it('has a method to find all transactions', function(){

    /*
    var repo = new TransactionRepository();

    expect(repo.find().length).toBeGreaterThan(0);
    */

  });
});
